[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "expense-tracker"
version = "0.0.1"
readme = "README.md"

[project.scripts]
expense-tracker = "models.main:main"

[tools.setuptools]
packages = ["models"]